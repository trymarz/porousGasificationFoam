FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "constant";
    object      lambdaDict;
}


lambdaMode constant; // or Ts  or constant
lambdaValue 0.5;


// lambdaDot
// {
//     type coded;
//     code
//     #{
//         return 0.1*sin(x);
//     #};
// }


// lambdaDot
// {
//     type coded;
//     code
//     #{
//         const scalar factor = 10;
//         return factor*x;
//     #};
// }


lambdaDot
{
    type coded;
    code
    #{
        
        return 0.001*x;
    #};
}


interpolateUs true; // true: interpolate UsDEM into a continuous Us field inside solid cells. false: use only raw UsDEM, so cells without sphere centers keep Us = 0.


solidPorosityCutoff 0.45; // Cells with porosityF below this value are treated as solid containing cells and receive interpolated Us. Larger value: more nearly-empty cells are included. Smaller value: only denser solid cells are included.
// better to keep it slightly higher than to initial porosity in the case
// porosity here = how empty a cell is . porosity =1 = no solid 

demVelocityAnchorCoeff 1e6; // constraint strength that keeps cells with sphere centers close to their raw UsDEM value. Larger value: occupied cells follow UsDEM more strictly. Smaller value: occupied-cell velocity is smoothed more by neighbors.
//Larger demVelocityAnchorCoeff strengthens the influence of DEM cells.

backgroundUsAnchorCoeff 1000; // small regularization for empty solid cells so the interpolation equation remains well posed. Larger value: empty solid cells are weakly pulled toward zero. Smaller value: interpolation spreads more freely from DEM cells.

nUsInterpolationCorrectors 2; // Number of interpolation/smoothing solves used to fill Us in solid cells without particles. Larger value: smoother and more diffused Us field. Smaller value: sharper, more local interpolation.










