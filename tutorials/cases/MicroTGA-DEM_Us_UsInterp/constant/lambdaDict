FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "constant";
    object      lambdaDict;
}

// lambdaDot is no longer prescribed here: the solver (volPyrolysis) computes
// it from the dlambdaOverDTs/massSplitBetweenLamAndPor coefficients in
// constant/solidThermophysicalProperties. This dictionary still configures
// the DEM->CFD solid-velocity interpolation: interpolateUs,
// solidPorosityCutoff, interpolationMode and its sub-model coefficients
// (defaults are used when absent).
