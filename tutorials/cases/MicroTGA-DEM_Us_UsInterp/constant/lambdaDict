FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "constant";
    object      lambdaDict;
}

// Stated explicitly (was implicit via the constant/no-lambdaValue default)
// so the case doesn't silently follow a future default change. This case is
// about Us interpolation only -- lambdaDot stays 0, YADE's lambda never
// moves off its 1.0 default.
lambdaMode constant;
