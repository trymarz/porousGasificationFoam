FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "constant";
    object      lambdaDict;
}

// lambdaDict configures ONLY the DEM->CFD solid-velocity interpolation
// consumed by lambdaDotModel: interpolateUs, solidPorosityCutoff,
// interpolationMode and its sub-model coefficients (defaults are used when
// absent, as here). The lambdaDot model selection (lambdaMode) and its
// coefficients live in constant/solidThermophysicalProperties.
