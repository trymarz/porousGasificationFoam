# flatPlate has no probe function object, so the probes panel is omitted.
[heatmap]
fields = ["porosityF", "Ts", "T", "p", "YporousMaterial", "Ydefault"]

[monitor]
refresh_interval = 2.0
panels = ["progress", "heatmap"]
